node_modules/
dist/
public/world/
artifacts/
data/
*.log
.DS_Store

# local save/state backups — never publish
ironfronts-backups/

# LOCAL PROTOTYPE ART — NOT FOR DISTRIBUTION. Copyrighted reference images
# (e.g. Call of War wiki unit art) used only as a local dev visual target.
# See dev-assets/callofwar-reference/README.md before touching this rule.
dev-assets/
